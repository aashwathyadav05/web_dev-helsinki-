sequenceDiagram
    participant User
    participant Browser
    participant Server

    Note over User, Browser: User opens https://helsinki.fi
    
    Browser->>Server: HTTP GET /spa
    Server-->>Browser: HTML Document
    
    Browser->>Server: HTTP GET main.css
    Server-->>Browser: CSS stylesheet
    
    Browser->>Server: HTTP GET spa.js
    Server-->>Browser: JavaScript file (spa.js)
    
    Note over Browser: Browser executes spa.js
    
    Browser->>Server: HTTP GET data.json
    Server-->>Browser: JSON data [{ "content": "...", "date": "..." }, ...]
    
    Note over Browser: Browser renders the notes using DOM-API
    
    User->>Browser: Types note and clicks "Save"
    
    Note over Browser: JavaScript catches submit event
    Note over Browser: e.preventDefault() stops page reload
    Note over Browser: New note added to local list & UI redrawn
    
    Browser->>Server: HTTP POST /new_note_spa
    Note right of Browser: Content-type: application/json<br/>Payload: { "content": "...", "date": "..." }
    
    Server-->>Browser: HTTP 201 Created
    
    Note over Browser: No further requests; UI is already updated
