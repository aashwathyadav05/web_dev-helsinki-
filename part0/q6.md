sequenceDiagram
    participant User
    participant Browser
    participant Server

    User->>Browser: Types note text (e.g., "vaz")
    User->>Browser: Clicks "Save" button

    Note over Browser: JavaScript catches "submit" event
    Note over Browser: e.preventDefault() stops full page reload
    
    Note over Browser: Local JS adds new note to existing list
    Note over Browser: redrawNotes() updates UI instantly
    
    Browser->>Server: HTTP POST /new_note_spa
    Note right of Browser: Content-type: application/json
    Note right of Browser: Payload: {"content": "vaz", "date": "2024-..."}

    Server-->>Browser: HTTP 201 Created
    
    Note over Browser: Success! No redirect or reload needed.
