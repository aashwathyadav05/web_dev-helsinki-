i read the HTML basics,containing various tags like :-
1)<p> tag --used for writing text as a paragraph in the page
2)<ul> tag --used as an unordered list 
3)<li> tag --used to enter the list item in the list
4)embedding the images using <img src="" />
5)<h1>,<h2>,<h3>,<h4>,<h5>,.... as various headers in the HTML page
6)<a> using href attribute  --how to insert hyperlinkslinks
